package com.flighttracking.config;

import org.springframework.context.annotation.Configuration;

/**
 * Central application configuration.
 * This class is intentionally minimal for Part 1.
 * Future modules (auth, flight, etc.) will add their own @Configuration classes
 * alongside this one without restructuring the package.
 */
@Configuration
public class AppConfig {
}
